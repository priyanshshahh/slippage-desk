alpaca-py>=0.33.0
python-dotenv>=1.0.1
pyyaml>=6.0.2
pandas>=2.2.0
numpy>=1.26.0
anthropic>=0.40.0
# Neural text to speech for the submission video's narration, run locally so
# the build needs no TTS account or API key. The voice model itself is a 63MB
# download into .voices/ (gitignored):
#   python -m piper.download_voices en_US-lessac-medium --data-dir .voices
piper-tts>=1.7.0
